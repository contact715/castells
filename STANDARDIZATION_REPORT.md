# 📋 Отчет о стандартизации сайта

## ✅ Уже стандартизировано

1. **Кнопки** - Все кнопки используют компонент `Button` с едиными стилями
2. **Секции** - Базовый компонент `Section` и `SectionContainer` существует

## ❌ Что нужно стандартизировать

### 1. Формы и инпуты ⚠️ КРИТИЧНО

**Проблема:** Разные стили инпутов в разных формах

- **ContactPage**: `rounded-xl px-4 py-3 text-base` ✅ (стандарт)
- **QuizForm**: `rounded-none border-b` ❌ (другой стиль)
- **GrowthAudit**: `rounded-xl px-4 py-3` ✅ (но разные цвета)
- **AILab**: `rounded-2xl py-8 px-8 text-xl` ❌ (слишком большой)

**Решение:** Использовать компонент `Input` с вариантами:
- `default` - для светлых форм
- `minimal` - для темных форм (QuizForm)
- `dark` - для GrowthAudit

---

### 2. Типографика ⚠️ ВАЖНО

**Проблема:** Разные размеры заголовков и текста

**Заголовки:**
- ✅ Стандарт: `text-3xl md:text-4xl lg:text-5xl` (SectionHeader)
- ❌ Есть: `text-2xl`, `text-4xl`, `text-6xl`, `text-7xl`, `text-8xl` без системы

**Body текст:**
- ❌ Разные: `text-sm`, `text-base`, `text-lg`, `text-xl` без стандарта

**Решение:** Использовать `designTokens.typography`

---

### 3. Spacing (отступы) ⚠️ ВАЖНО

**Проблема:** Много разных значений

**Section padding:**
- ✅ Стандарт: `py-24 md:py-32` (Section)
- ❌ Есть: `py-20`, `py-32`, `pt-32 pb-20`, `pt-32 pb-32`

**Container padding:**
- ✅ Стандарт: `px-6` (SectionContainer)
- ❌ Есть: `px-8`, `px-10`, `px-12`

**Gap:**
- ❌ Много разных: `gap-2`, `gap-3`, `gap-4`, `gap-6`, `gap-8`, `gap-12`, `gap-16`, `gap-20`, `gap-24`

**Card padding:**
- ❌ Разные: `p-6`, `p-8`, `p-10`, `p-12`, `p-16`

**Решение:** Использовать `designTokens.spacing`

---

### 4. Скругления ⚠️ СРЕДНЕ

**Проблема:** Разные значения rounded

- ✅ Стандарт: `rounded-xl` (кнопки, инпуты)
- ❌ Есть: `rounded-2xl`, `rounded-3xl`, `rounded-[2rem]`, `rounded-[32px]`

**Решение:** Использовать `designTokens.borderRadius`

---

### 5. Карточки ⚠️ СРЕДНЕ

**Проблема:** Разные стили карточек

**Padding:**
- ❌ `p-8`, `p-10`, `p-12`, `p-16`

**Скругления:**
- ❌ `rounded-xl`, `rounded-2xl`, `rounded-3xl`, `rounded-[2rem]`

**Тени:**
- ❌ `shadow-sm`, `shadow-lg`, `shadow-xl`, `shadow-2xl`

**Решение:** Использовать `designTokens.cardClasses`

---

### 6. Бейджи и метки ⚠️ НИЗКО

**Проблема:** Разные стили бейджей

- ✅ Есть стандарт в SectionHeader
- ❌ Но используется вручную в разных местах

**Решение:** Использовать компонент `Badge`

---

### 7. Цвета и границы ⚠️ НИЗКО

**Проблема:** Разные значения opacity для границ

- ❌ `border-black/5`, `border-black/10`, `border-black/20`
- ❌ `border-white/5`, `border-white/10`, `border-white/20`

**Решение:** Использовать `designTokens.colors.border`

---

## 📊 Приоритеты

1. **КРИТИЧНО** - Формы и инпуты (влияет на UX)
2. **ВАЖНО** - Типографика и Spacing (влияет на визуальную консистентность)
3. **СРЕДНЕ** - Карточки и скругления (влияет на визуальную консистентность)
4. **НИЗКО** - Бейджи и цвета (косметика)

---

## 🎯 План действий

1. ✅ Создать компонент `Input`
2. ✅ Создать компонент `Badge`
3. ✅ Создать `designTokens.ts`
4. ⏳ Применить `Input` во всех формах
5. ⏳ Стандартизировать типографику
6. ⏳ Стандартизировать spacing
7. ⏳ Стандартизировать карточки

---

## 📝 Компоненты созданы

- ✅ `components/ui/Input.tsx` - Стандартизированный инпут
- ✅ `components/ui/Badge.tsx` - Стандартизированный бейдж
- ✅ `lib/designTokens.ts` - Дизайн-токены




