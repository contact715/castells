import type React from 'react';

/*
  Разметка для поисковиков. Компонент ОТКЛЮЧЁН 24 августа 2026, возвращает
  ничего. Разметку кладёт в HTML генератор статических страниц —
  scripts/prerender-pages.mjs, функция buildSchema.

  ЗАМЕР, из-за которого это сделано. На странице цен готовый HTML нёс ДВА
  блока разметки, а после того как приложение дорисовывало свои, их
  становилось СЕМЬ:

    Organization     ×2  — с разным набором полей
    BreadcrumbList   ×3  — два из них байт в байт одинаковые
    WebSite          ×1
    FAQPage          ×1

  Значения нигде не противоречили, и это единственная причина, по которой
  беда была тихой: поиск видел две записи об одной конторе и три описания
  одного пути, но не мог поймать нас на разных ответах. До первой правки в
  одном из двух мест.

  ЧТО ПЕРЕНЕСЕНО, А НЕ ВЫБРОШЕНО. Приложение давало три типа, которых у
  сервера не было. Два переехали:

    Article  — теперь с НАСТОЯЩЕЙ датой из data/blog.ts. Здесь она
               подставлялась сегодняшней, если вызывающий её не передал, то
               есть статья каждый день заявляла поиску, что вышла сегодня.
    Service  — теперь без придуманного предложения: здесь стоял offers с
               описанием «Professional digital marketing services» и без
               единой цены. Предложение без цены — это не предложение.

  ЧТО ВЫБРОШЕНО СОЗНАТЕЛЬНО. Блок WebSite объявлял поисковику адрес поиска
  по сайту: castells.media/search?q={строка}. Проверено на живом сайте —
  и /search?q=hvac, и /search отвечают 404, такого маршрута в приложении нет
  вовсе. Мы обещали поиску возможность, которой у нас нет. Если она
  понадобится, сначала делается настоящая страница поиска, и только потом
  блок разметки — в этом порядке, а не наоборот.

  ПОЧЕМУ КОМПОНЕНТ ОСТАВЛЕН ЗАГЛУШКОЙ, А НЕ УДАЛЁН. Его зовут из двадцати
  файлов. Удаление — это двадцать правок в разметке страниц ради нулевого
  изменения того, что видит человек и робот. Заглушка стоит ноль байт в
  собранном коде: сборщик выкидывает вызовы, которые ничего не возвращают.
  Убирать вызовы можно спокойно, по одному, когда рядом и так идёт работа.

  Ровно так же 24 августа поступили с компонентом SEO по той же причине.
*/

interface SchemaMarkupProps {
  type?:
    | 'Organization' | 'WebSite' | 'Service' | 'Article' | 'BreadcrumbList'
    | 'FAQPage' | 'LocalBusiness' | 'HowTo' | 'VideoObject' | 'ItemList'
    | 'Review' | 'Definition' | 'Course';
  data?: Record<string, unknown>;
}

const SchemaMarkup: React.FC<SchemaMarkupProps> = () => null;

export default SchemaMarkup;
