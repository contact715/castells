# Ежедневная работа по сайту

Файл, по которому работает утренняя задача. Он же журнал: сделанное отмечается
здесь, чтобы работа не повторялась и не терялась между днями.

Заведён 23 августа 2026 по просьбе владельца: «создай задачу на каждый день,
основная задача по SEO двигать сайт, будем прорабатывать каждый аспект,
улучшать процесс, анализировать текущее состояние».

---

## Что делать каждое утро

Порядок жёсткий, отступать от него нельзя.

**1. Замер (5 минут).** Прежде чем что-то делать, посмотреть, в каком сайт
состоянии сейчас:

```bash
node scripts/seo-audit.mjs
```

Прибор проверяет живой сайт, а не код: доступность страниц, дубли заголовков,
пустые описания, битые адреса, вес и скорость главной. Если он краснеет —
чинить это, а не браться за очередь.

**2. Одна работа из очереди.** Взять верхнюю незакрытую задачу из раздела
«Очередь» ниже. Не хвататься за несколько: одна доведённая до прода лучше трёх
начатых.

**3. Проверка на проде.** Ничего не считается сделанным, пока не проверено на
живом сайте. Не «собралось» и не «должно работать» — открыть адрес и убедиться.

**4. Отметка в журнале.** Внизу файла, одной строкой: дата, что сделано, чем
подтверждено.

**5. Отчёт владельцу.** Коротко и простым языком: что замерил, что сделал, что
изменилось, что дальше. Без технического жаргона.

---

## Про посты в блог

Пост выходит не по расписанию, а когда есть о чём написать. Пустой пост вредит
сильнее, чем его отсутствие: мы только что вычистили из блога девять таких.

**Пост можно писать, если есть хотя бы одно из этого:**

- наш собственный замер (расход рекламы, сроки, цены, результат клиента с его
  согласия);
- разбор того, как мы работаем, с конкретикой, которую можно проверить;
- ответ на вопрос, который реально задают клиенты, с нашим опытом внутри.

**Третий источник тем — рынок.** Раз в неделю смотреть, что происходит в ИИ
для маркетинга и в сервисном бизнесе. Если случилось что-то, что реально
касается подрядчика (Google меняет правила рекламы, появляется инструмент,
меняются требования к профилям), пишем разбор.

Формат строгий: **не пересказ новости, а что это значит для владельца
HVAC-компании**. Ссылка на источник с датой обязательна. Если своего вывода
нет — не публикуем: пересказ чужого текста не добавляет ничего, а мы такое
уже вычищали. Ориентир: не чаще раза в неделю.

**Нельзя:** пересказ чужих статей без нашего вывода, «тренды 2026», числа без
источника, советы, которых мы сами не применяли.

Если темы нет — день уходит на очередь, и это нормально. Ориентир: два-три
поста в неделю, а не семь.

**Правило файла данных блога** (`data/blog.ts`) повторяется здесь: число без
источника в статью не идёт.

---

## Академия для подрядчиков (/academy)

Идея владельца, 24 августа 2026: отдельная бесплатная школа для
предпринимателей в нишах, с которыми работает агентство (constraction,
HVAC, appliance repair и похожие) — как запустить и вести такой бизнес в
США. Не рекламный блок на сайте, а отдельная организация под своим именем
— «академия».

**Статус на 27 августа: запущена и публикуется, три открытых вопроса
закрыты.** Владелец 26 августа ответил на все три вопроса ниже (раздел на
сайте, не поддомен; тексты пишем мы сами из своей практики; простой текст,
без видео/PDF). Правило подтверждения из прошлой версии этого файла («ничего
не публикуется без одобрения») снято для уже одобренной программы —
дальше идёт по решению от 26 августа, без переспрашивания на каждый урок.
Не отменяет общих правил сайта: без выдуманных цифр, без юридических
советов «делайте так», без ourWork на клиента, чей сайт не проверен (см.
`constants.ts` → `BUILT_SITES`).

**Устройство.** `/academy` — общий хаб. Дальше пять курсов по ремеслу
(`/academy/<ремесло>`), внутри каждого шесть уроков по одним и тем же
шести модулям (запуск бизнеса → бренд → сайт → куда идут деньги → как
находят → продажи и дожим). Данные все в одном файле, `data/academy.ts`.

**Прогресс по курсам, 27 августа:**
- [x] contractors (общий, для всех ремёсел) — 6 уроков, готово 26 августа
- [x] hvac — 6 уроков, готово 26 августа
- [x] remodeling (ремонт и строительство) — 6 уроков, готово 26 августа
- [x] automotive (детейлинг, плёнка, тонировка, керамика) — 6 уроков,
  готово 27 августа. Без картинок, схемы переиспользованы готовые
  (`six-things`, `three-encounters` и так далее — они не привязаны к
  ремеслу), без примера из своей практики: проверенного сайта в этой нише
  у нас нет (см. ниже).
- [x] professional (страхование, юридические услуги, консалтинг,
  финансовое планирование, мед-спа) — последний из пяти, готово
  27/28 августа (см. запись ниже и в очереди). Строка выше не была
  обновлена, когда курс закрыли — поправлено 29 августа, чтобы прогресс
  не расходился с очередью.

**Поддомен academy.castells.media.** Владелец решил 26 августа: не
отдельный сайт, раздел на основном домене — поиск иначе считает поддомен
отдельным сайтом, и вес уроков не достался бы основному домену. Сейчас
`academy.castells.media` ВРЕМЕННО перенаправляет на `/academy` (не 301,
чтобы решение можно было откатить). Заработает после того, как владелец
добавит домен в проект на Vercel и заведёт запись DNS — записи пока нет,
проверено 26 августа.

**Пример из практики для automotive не добавлен, и почему.** У нас есть
кейсы в этой нише (Drive Car Studio, Los Angeles; Detailed Exotics
Portland) — они в `constants.ts` → `CASE_STUDIES`, оба «клиенты по
рекламе». Но в `BUILT_SITES` (сайты, которые построили МЫ) их нет: адрес
Detailed Exotics неизвестен, «спрошен у владельца» — комментарий в файле
от 22 августа. Пока адреса нет, блок ourWork в уроках automotive не
заполняется: страница без проверяемого факта хуже страницы без блока.

## Очередь

Задачи по убыванию пользы. Сделанное отмечать `[x]` и переносить в журнал.

### Ждёт только нас

- [x] **Академия: черновая структура программы.** Сделано 25 августа:
  docs/ACADEMY_DRAFT.md — семь модулей, собранные из брифа владельца и того,
  что уже пересказано в разделе «Академия для подрядчиков» ниже (пропозал GC
  Sacramento в репозитории не лежит, только его пересказ). Два модуля («запуск
  бизнеса», «основы маркетинга») и весь модуль 7 помечены как требующие
  уточнения — содержание там не раскрыто ни в брифе, ни в пропозале, и
  придумывать его не стал. Никуда не публиковалось, только черновик в
  репозитории на утверждение. Дальше — ждёт владельца (три вопроса ниже плюс
  уточнение по модулям 1, 4 и 7).

- [x] **Свои заголовки и описания 41 странице.** Сделано 23 августа. Двадцать одна услуга и двадцать
  ниш сейчас отдают один и тот же заголовок «Castells Media | Marketing agency
  for home service businesses». Для поиска это 41 одинаковая страница, они
  конкурируют между собой. Делать порциями по 5–7 в день, выводя заголовок из
  названия услуги или ниши. Проверять на проде: `curl -s <адрес> | grep title`.
- [x] **Меню в шапке.** Сделано 23 августа: пять пунктов Work, Services, Prices,
  About, Contact, без выпадающих списков. Проверено на проде, все адреса ведут
  на живые страницы.
- [x] **Редиректы старых адресов.** Сделано 24 августа. Логов трафика не
  оказалось (тариф hobby, веб-аналитика не включена), поэтому список мёртвых
  адресов собран по истории репозитория, а не по логам. Заодно вскрылось, что
  несуществующие адреса отдавали 200 и копию главной вместо 404.
- [x] **Локальная страница Roseville.** Сделано 24 августа: /roseville-marketing-agency,
  с адресом, телефоном, юрлицом, клиентами в Калифорнии и ценами.
- [x] **Три страницы-ответа.** Сделано 24 августа: хаб /learn и три вопроса под
  ним, у каждого блок «How we know» с нашим фактом.
- [x] **Перелинковка.** Сделано 24 августа: робот видел 5 страниц из 74, теперь
  все нужные достижимы, максимум два клика от главной.
- [x] **Замер точки отсчёта.** Сделано 24 августа, записан в
  docs/AI_SEARCH_BASELINE_2026-08-24.md. Повторить 24 сентября.
- [x] **Своя разметка для /careers и /company.** Сделано 24 августа —
  выяснилось, что чинить нечего: обе страницы целиком выдуманные (стаж
  «12+ years», «500+ проектов», «$50M+ выручки», вымышленная команда
  «ex-Google/ex-Meta», вымышленные вакансии). Страницы удалены, адреса
  уведены 301-м на /about.
- [x] **Адреса ниш с косой чертой и амперсандом.** Сделано 25 августа: `slugify`
  только заменял пробелы на дефис, поэтому «Int/Ext Painting» давал
  /industries/int/ext-painting (лишний уровень вложенности, canonical на саму
  себя, битый адрес), а «ADU & Additions» — /industries/adu-&-additions.
  Оказалось, что баг жил в ДВУХ местах: `lib/routes.ts` (используется в
  браузере) и своя копия той же логики в `scripts/prerender-pages.mjs`
  (собирает статику) — правки только в одном месте было бы недостаточно.
  `&` теперь читается как «and», `/` — как разделитель слов. Затронуты 4 из
  20 ниш: adu-and-additions, fencing-and-gates, flooring-and-tile,
  int-ext-painting. Старые кривые адреса уведены 301-м в vercel.json.
- [ ] **Повторить замер в поиске раньше 24 сентября.** 24 августа найден и
  устранён баг, из-за которого браузер (и, возможно, робот Google при
  рендеринге) видел не те заголовки и описания, что лежат в HTML — компонент
  `<SEO>` их перебивал после загрузки скриптов. Baseline от 24 августа
  («ноль страниц в site:castells.media») мог быть снят ДО этого исправления
  и не отражать его эффект. Проверить `site:castells.media` через 1-2 недели
  после выкатки, не дожидаясь месячного цикла.
- [x] **Описания длиннее 165 знаков.** Запись от 25 августа ниже была
  ОШИБОЧНОЙ, найдено и исправлено 27 августа: самопроверка «описания не
  длиннее 165 знаков» проверяла не весь сайт, а только статический список
  из `page-meta.mjs` — примерно четверть страниц. Услуги, ниши, статьи,
  ответы, вся академия и кейсы (три четверти сайта) в неё не попадали, и
  зелёный результат 25 августа ничего не доказывал про них. После починки
  самопроверки (теперь смотрит на тот же список, что собирает сборка)
  нашлось 18 длинных заголовков и 43 длинных описания. У 18 ниш причина
  одна на всех — шаблон описания добавлял фразу под сто символов перед
  адресом, сокращён. Остальное — уроки академии (все четыре курса) и
  четыре кейса — укорочены вручную. Подтверждение: `node
  scripts/prerender-pages.mjs --self-test` 53/53, на проде заголовки и
  описания в HTML проверены на нескольких адресах из разных семейств
  (ниша, урок академии, кейс).
  *(исходная, ошибочная запись от 25 августа оставлена ниже для истории)*
- [x] ~~Описания длиннее 165 знаков.~~ Проверено 25 августа: самопроверка
  сборки (`node scripts/prerender-pages.mjs --self-test`) сейчас даёт 51/51,
  включая «описания не длиннее 165 знаков». Похоже, это уже почищено
  побочным эффектом правки заголовков и описаний 23-24 августа — отдельно
  чинить было нечего.
- [x] **Курс академии для automotive (детейлинг, плёнка, тонировка,
  керамика).** Сделано 27 августа: шесть уроков, `data/academy.ts`,
  подробности в разделе «Академия для подрядчиков» выше. Четвёртый из пяти
  курсов, пятый (professional services) не начат.
- [x] **Курс академии для professional services** (страхование, юридические
  услуги, консалтинг, финансовое планирование, мед-спа) — последний из пяти
  курсов, сделан 27 августа отдельной (параллельной) сменой, подробности в
  журнале за 28 августа ниже. Академия укомплектована полностью: пять
  курсов, тридцать уроков.
- [x] **Рекламный пафос и недоказанные заявления вне числового формата.**
  Сделано 29 августа. Проверил `ServicePage.tsx` отдельно — FAQ там уже
  честный (видимо, починили в незалогированной правке между 28 и 29
  августа), править было нечего. Осталось два места, оба поправлены:
  `AllIndustriesPage.tsx` — 5 из 6 карточек блока «Why industry expertise
  matters» переписаны с недоказуемых заявлений («we know exactly who your
  ideal customers are», «outmaneuver them», «we know what good looks
  like», «connections… specific to your vertical») на честные формулировки
  без выдуманного всезнания («мы уже строили кампании в этой нише», «где
  есть контакты с поставщиками — используем, где нет — скажем прямо»).
  `IndustryPage.tsx` — два FAQ-пункта на всех 20 страницах ниш
  («What marketing strategies work best», «How do you target customers»)
  заменены с общих слов без содержания («data-driven targeting»,
  «conversion-optimized landing pages», «unique customer journey») на
  конкретный честный ответ, тем же тоном, что уже стоит в соседних
  пунктах того же FAQ («сайт, реклама, профиль, дожим» и «по геолокации и
  по тому, что реально ищут»). Чисел не добавлял, фактов о клиентах не
  выдумывал. Подтверждение до выкатки: `npx tsc --noEmit` — те же 6 старых
  ошибок, ни одной новой; `node scripts/prerender-pages.mjs --self-test`
  53/53; `npm run build` зелёный. Этот текст рисуется в браузере после
  гидратации (как и находки 28 августа), поэтому в статическом
  `dist/*/index.html` его не видно. Проверка на проде (коммит 397be55,
  `mcp__Vercel__web_fetch_vercel_url`): нашёл реальные хеши бандлов
  `AllIndustriesPage-Ub6O9of5.js` и `IndustryPage-zp-I9tsW.js` (свежие,
  `age: 0`) через карту импортов внутри `index-DDpv2Ymq.js`, прогнал через
  grep — все новые формулировки на месте, ни одной старой строки.
- [x] **«dominate», выдуманный рейтинг «5.0» и обещание «24 часа» на
  /thank-you.** Сделано 30 августа при ручном разборе кода (тот же способ,
  что 28 августа — предыдущая находка «Рекламный пафос…» проверяла
  страницы услуг и ниш, но не главную и не служебные страницы).
  `components/sections/Services.tsx` на главной (компонент рисуется в
  браузере после гидратации, поэтому его не видно в статическом
  `dist/*/index.html` и не ловит замер): «we build the infrastructure your
  business needs to dominate» заменено на «…needs to grow».
  `components/pages/ThankYouPage.tsx` (страница `/thank-you`, noindex, но
  видна живым посетителям после отправки формы) — три отдельные находки в
  одном файле: «Quick response guaranteed» и «Response within 24 hours» —
  обещания без срока и источника, того же класса, что уже чистили в FAQ и
  на страницах ниш 28 августа, заменены на «We'll get back to you soon» и
  «We reply personally»; блок «Social Proof» внизу страницы рисовал ПЯТЬ
  закрашенных звёзд и текст «5.0» безусловно, при том что реальных отзывов
  на сайте нет ни одного (открытый пункт очереди «Отзывы» ниже) — это
  ровно то, что запрещено правилом «не выдумывать рейтинги и счётчики»,
  блок удалён целиком, осталась только честная строка «Thanks — we'll be
  in touch shortly». Первую находку (Services.tsx) выкатил и проверил на
  проде отдельным деплоем, вторую и третью (ThankYouPage.tsx, три правки)
  — вторым деплоем того же дня, обе проверки ниже в журнале. Все находки —
  единственные хиты по словам dominate/comprehensive/seamless/guarantee, по
  числам вида «NNN+» и по «24 hours» в живом коде (components, data,
  constants.ts); остальные упоминания — комментарии про уже исправленные
  находки прошлых дней, не живой текст.

### Ждёт владельца

- [ ] **Отзывы.** Пять коротких сообщений клиентам с отметкой «Полностью
  доволен» в Monday: Roman's Service, HVAC & Appliance Repair Clinic LA, AAA
  Brothers, Design by Nova, Detailed Exotics. Без них на сайте нет ни одного
  отзыва.
- [x] **Форма заявки не доставляет письмо.** Частично закрыто 2 сентября.
  formsubmit.co (промежуточная починка 23-24 августа) оказался сломан
  по-новому — требовал ручной активации по письму и показывал посетителю
  сырой текст ошибки. Заменён на свои обработчики `/api/contact` и
  `/api/quiz` (`api/contact.js`, `api/quiz.js`, `api/_lib/notify-lead.js`),
  доставляющие заявку в Telegram и на почту параллельно. **Telegram работает
  прямо сейчас** — переиспользован уже существующий бот `@castells_bot`
  (тот же, что шлёт отчёты по рекламе клиентов), проверено боевой отправкой
  через настоящую форму на сайте. **Почта всё ещё ждёт владельца** — нужен
  аккаунт на resend.com и `RESEND_API_KEY`, создание аккаунта не в моих
  полномочиях. Подробности: [[castells-broken-forms]] в памяти сессии.
- [x] **Доступ к Google Search Console.** Закрыто 2 сентября. Свойство
  `https://www.castells.media` добавлено (тип URL prefix), подтверждено
  автоматически — Google уже видел DNS-запись владельца. Карта сайта
  отправлена, статус «Success», 87 страниц найдено. Индексация страниц ещё
  обрабатывается Google (обычно 1-3 дня для новых свойств) — статус «why
  pages aren't indexed» пуст, но помечен «processing».
- [ ] **Доступ к Google Business Profile.** Для локального поиска это половина
  дела: категории, фотографии, отзывы, ответы на них.
- [x] **Домен для академии — Vercel-часть.** Закрыто 2 сентября:
  `academy.castells.media` добавлен в проект `castells` на Vercel, редирект
  на `/academy/*` уже стоял в vercel.json заранее. **DNS-часть всё ещё ждёт
  владельца** — A-запись `academy` → `76.76.21.21` у регистратора (GoDaddy),
  без неё адрес физически никуда не ведёт.
- [ ] **Аналитики нет никакой.** Ни счётчика, ни данных о посетителях —
  тариф Vercel hobby без веб-аналитики. Отмечено в docs/SESSION_STATE.md
  26 августа, сюда не попадало.

---

## Правила, которые нельзя нарушать

1. **Число без источника на сайт не идёт.** Ни в текст, ни в разметку, ни в
   заголовок. Если числа нет — пишем без числа.
2. **Не выдумывать факты о клиентах.** Имя, ниша, город, результат — только из
   Monday или от владельца.
3. **Проверять на проде.** Собранная сборка ничего не доказывает.
4. **Не ломать то, что работает.** Перед правкой маршрутов и тегов — замер, после
   — замер. Сравнить.
5. **Говорить, чего не сделал.** Если задача не закрыта, сказать прямо, а не
   растворять в отчёте.

---

## Журнал

Одна строка на день: дата, что сделано, чем подтверждено.

- 2026-08-23 — заведён этот план и утренняя задача. Подтверждение: файл в репозитории.
- 2026-08-23 — меню в шапке: пять пунктов вместо Cases/Company с выпадающими
  списками, файл шапки 805 строк → 210. Подтверждение: на проде Work, Services,
  Prices, About, Contact, выпадающих списков ноль.
- 2026-08-24 — статья «We checked whether our own site was in Google. It was not»
  на вчерашнем замере: инструкция, которую читатель может выполнить сам, и три
  наши собственные находки. По дороге всплыл мой же дефект: редирект со старых
  адресов 4-9, поставленный при чистке блога, перехватил новую статью, занявшую
  номер 4. Сужен до 5-9. Подтверждение: на проде все четыре статьи отдают свои
  заголовки, похожесть с остальными ниже порога.
- 2026-08-24 — замер точки отсчёта. Главное: site:castells.media не возвращает
  НИ ОДНОЙ страницы сайта, по бренду находят реестр компаний и чужие агентства,
  по целевому запросу выдаются Social Cali и QuantiMedia. Найдены и устранены
  две технические причины: оба адреса (с www и без) отвечали 200 без
  перенаправления, то есть дубли для поиска; llms.txt описывал прежний сайт со
  старым именем и обещанием удалённых цифр. Подтверждение: на проде
  castells.media отдаёт 308 на www, все ключевые страницы 200, llms.txt новый.
- 2026-08-24 — перелинковка. Замер вскрыл, что из 74 страниц роботу по ссылкам
  в HTML были доступны ПЯТЬ: все переходы на сайте рисуются скриптами, и работа
  двух прошлых дней держалась только на карте сайта. Генератор теперь кладёт
  связи в HTML: хабы на детей, страницы обратно на хабы и соседей, главная на
  все разделы. Подтверждение: недостижимых из тех, что должны быть в поиске,
  ноль; максимум два клика от главной; на проде главная отдаёт 19 ссылок,
  работы 23, услуги 18.
- 2026-08-24 — три страницы-ответа и хаб /learn: нужен ли сайт при странице в
  Facebook, чем агентство занято каждый месяц, обязателен ли долгий договор. У
  каждой блок «How we know» с нашим фактом. Подтверждение: на проде свои
  заголовки и разметка вопроса-ответа, похожесть между собой 54%, с блогом 43%
  при пороге 60%.
- 2026-08-24 — локальная страница /roseville-marketing-agency: адрес, телефон,
  юрлицо, четыре направления работ, клиенты в Калифорнии, цены. В тексте прямо
  сказано, что калифорнийские клиенты не из Roseville, а из Лос-Анджелеса.
  Подтверждение: на проде свой заголовок, H1 и адрес, 1194 символа текста видны
  роботу без скриптов; карта сайта 35 адресов.
- 2026-08-24 — разметки для поисковиков не было НИ НА ОДНОЙ странице: её
  добавляли скрипты, и робот без выполнения скриптов не видел ничего. Теперь
  генератор кладёт её в HTML на все 70 страниц. Подтверждение: на проде блоки
  ld+json есть на главной, контактах, услугах и локальной странице; 139 блоков
  разбираются как корректный JSON, ошибок ноль.
- 2026-08-23 — свои заголовки, описания и тексты страницам услуг, ниш и статей.
  Было 26 страниц с собственными тегами, стало 69. Замер похожести показал
  62-78% совпадения, поэтому применено правило «нет актива — нет страницы в
  поиске»: из 20 ниш кейсами обеспечены 2, из 20 услуг в поиск идут 8. Разведены
  две пары близких услуг. Подтверждение: `node scripts/seo-audit.mjs` на проде —
  находок нет, дублей заголовков нет, 34 адреса карты отвечают 200.
- 2026-08-24 (утро) — смена не состоялась: рабочее окружение не выпускает
  наружу ни один внешний адрес, включая castells.media, google.com и
  vercel.com — прокси сессии отвечает 403 на CONNECT ко всем трём
  («gateway answered 403 to CONNECT», см. `/root/.ccr/__agentproxy/status`).
  Это блокировка на уровне сессии, не сбой самого сайта: GitHub при этом
  доступен (git fetch/push работают через отдельный канал). Замер запустить
  нельзя, ни одну правку проверить на проде нельзя, поэтому ни замер, ни
  задача из очереди сегодня не сделаны — правило «не считается сделанным без
  проверки на живом сайте» не позволяет закрыть что-либо вслепую. На
  следующую смену нужно, чтобы доступ сессии к внешним адресам починили.
- 2026-08-24 (дополнение к записи выше) — обходной путь найден, смена
  доделана. Прямой доступ curl-ом действительно закрыт, но живой сайт
  достаётся через Vercel (`web_fetch_vercel_url`), а состояние выкаток — через
  их же API. Проверка на проде этим и делалась. На будущее: прибор
  `scripts/seo-audit.mjs` в такой сессии не работает (ходит напрямую), а
  проверка через Vercel — работает.
- 2026-08-24 — удалённые адреса отдавали 200 и копию главной вместо 404. В
  vercel.json с одностраничных времён лежали правила `/services/(.*)`,
  `/industries/(.*)` и `/blog/(.*)` → `/index.html`. После появления
  предрендера каждая живая страница лежит отдельным файлом и отдаётся раньше
  этих правил, поэтому сработать они могли только на несуществующем адресе — и
  отвечали кодом 200, копией главной, с «index, follow» и canonical на главную.
  Замер до правки: `/blog/7` (статья удалена при чистке блога) отдавал 200 и
  заголовок главной. Правила убраны, шесть удалённых статей `/blog/4`…`/blog/9`
  уведены 301-м на `/blog`, `/case-studies` — на `/work`. Сборка теперь падает,
  если такое правило вернётся. Подтверждение на проде после выкатки: `/blog/7`
  приводит на живой блог (canonical `/blog` вместо `/`), `/services/this-service-does-not-exist`
  отдаёт 404 с noindex, а живые страницы целы — `/services/meta-ads`,
  `/industries/hvac-systems` и `/blog/3` со своими заголовками и своими
  каноническими адресами.
- 2026-08-24 — замер точки отсчёта в поиске и перенаправление на один адрес
  (отдельная смена, коммит 1285dc2): `site:castells.media` не возвращает ни
  одной страницы, сайт не в индексе; castells.media и www отвечали 200 оба,
  добавлено перенаправление на www; переписан llms.txt.
- 2026-08-24 (утро, вторая смена) — сессия без прямого доступа в интернет
  (та же блокировка прокси, что и накануне: `castells.media` отвечает 403 на
  CONNECT). Прибор `scripts/seo-audit.mjs` не работает по той же причине, что
  описана в записи ниже. Проверка на проде велась через
  `mcp__Vercel__web_fetch_vercel_url`.
- 2026-08-24 — найдена и устранена САМАЯ важная причина, по которой поиск не
  видит сайт: компонент `<SEO>` (react-helmet-async) на каждой странице
  ПОСЛЕ загрузки скриптов молча перебивал заголовок, описание, canonical и
  OG-теги — те самые, что несколько дней чинили в prerender-pages.mjs —
  собственными, часто выдуманными. Проверено headless-браузером на собранной
  сборке: `/industries/hvac-systems` без JS отдавал «HVAC Systems marketing |
  Castells Media», после гидратации — другой заголовок и другое описание.
  Раз в HTML на каждой из 74 страниц уже стоят верные теги, компонент
  отключён (возвращает null), а не переписан — переписывать нечего.
  Это может быть причиной того, что `site:castells.media` не находил ничего
  в замере точки отсчёта: то, что видел прибор (HTML), и то, что видел
  браузер (после JS), было разным.
- 2026-08-24 — найдены и убраны вымышленные факты, которые из-за бага выше
  были не только в HTML для ботов, но и на экране у живых посетителей:
  - `/careers` и `/company`: несуществующие вакансии, стаж «12+ years»,
    «500+ проектов», «$50M+ выручки», команда «4 ex-Google, 3 ex-Meta»,
    статья «How We Generated $2.4M for a Roofing Company» — таких клиентов и
    статей нет. Обе страницы удалены целиком, адреса уведены 301-м на
    /about; ссылка «Careers» убрана из поиска по сайту.
  - `/case-studies/:id` с несуществующим id отдавал целиком выдуманный
    кейс: клиент «Apex Architecture», отзыв от несуществующей «Sarah
    Jenkins, CEO», цифры «$697k выручки +210%». Теперь при отсутствии кейса
    страница честно говорит, что его нет, и ведёт на /work.
  - JSON-LD Organization и LocalBusiness (были на каждой странице):
    описание «dominate their local markets», выдуманный адрес «1234 Main
    Street, 90401» и координаты Санта-Моники вместо Roseville — на
    /contact, самой важной для локального SEO странице. Заменено на
    реальный адрес из config/business.ts, лишние координаты убраны (не
    подтверждены — не публикуем).
  - Слово «dominate» (запрещено правилами) в заголовках на живых страницах:
    секция Industries на главной, хаб /industries и все 20 страниц ниш —
    CTA переписаны на простой язык без цифр и без «мы поможем доминировать».
  - Слово «comprehensive» (тоже запрещено): в шаблоне FAQ на 20 страницах
    услуг (там же был выдуманный «минимум $3000/мес» и сроки «2-4 недели» —
    без источника) и в текстах шести реальных кейсов в constants.ts.
  - `og-image.png` — картинка, которая показывается при пересылке ссылки на
    сайт в мессенджерах, — писала «Santa Monica, California» вместо
    Roseville. Перерисована с тем же дизайном и верным городом; заодно
    удалён неиспользуемый og-image.svg с текстом «Dominate Your Market».
  Подтверждение на проде после выкатки (коммит e24f516): `/careers` и
  `/company` отдают 301 на `/about`; `og-image.png` на проде весит ровно
  столько байт, сколько новый файл, с временем правки от выкатки;
  `/contact` без скриптов — адрес 1298 Antelope Creek Drive, ни «1234 Main
  Street», ни «90401» в HTML нет; `/industries/hvac-systems` без скриптов —
  слова «dominate» в HTML нет. Гидратационный баг (SEO.tsx) проверен
  headless-браузером на этой же сборке до выкатки: `npm run build` не
  сломан, самопроверка сборки 17/18 — как и было, без новых поломок.
- 2026-08-24 — в план добавлен новый раздел «Академия для подрядчиков»
  (academy.castells.com) со слов владельца: бесплатная школа для
  предпринимателей в нишах агентства. Пока только план и открытые вопросы,
  ничего не строилось и не публиковалось. Подтверждение: раздел и пункты
  очереди в этом файле.
- 2026-08-25 — замер: `scripts/seo-audit.mjs` снова не смог достучаться
  напрямую (та же блокировка прокси на CONNECT к castells.media, что и
  24 августа). Обходной путь тот же — `mcp__Vercel__web_fetch_vercel_url`.
  Проверены вручную /, /work, /services, /pricing, /about, /contact, /blog и
  sitemap.xml — все 200, свои заголовки, H1 и canonical, поломок не найдено.
- 2026-08-25 — академия: черновая структура программы, docs/ACADEMY_DRAFT.md.
  Подробности в пункте очереди выше.
- 2026-08-26 — замер: `scripts/seo-audit.mjs` снова не смог достучаться напрямую
  (та же блокировка прокси на CONNECT к castells.media). Обходной путь —
  `mcp__Vercel__web_fetch_vercel_url`: проверены /, /work, /services, /pricing,
  /about, /contact, /blog, /services/meta-ads, /industries/hvac-systems,
  /blog/1, /sitemap.xml (38 адресов, все живые) и заведомо несуществующий
  адрес — все 200, дублей заголовков нет, у каждой свои описание и H1, 404
  честный (не копия главной). Дополнительно — вне обычного набора прибора —
  проверил то, что раньше не смотрели: alt-тексты у всех 16 мест с `<img>` в
  коде (везде заполнены), robots.txt (корректный, ссылается на sitemap),
  совпадают ли слаги услуг и ниш с исправлением от 25 августа (совпадают,
  регрессии нет), самопроверку сборки `node scripts/prerender-pages.mjs
  --self-test` (51/51). Поломок не найдено, правок в код не вносил.
  Очередь: раздел «Ждёт только нас» пуст — всё сделано, кроме повторного
  замера в поиске, для которого рано (нужно 1-2 недели после выкатки от
  24 августа, сегодня только второй день); раздел «Ждёт владельца» упирается
  в доступы и решения, которые я не могу взять на себя сам (в частности,
  писать реальным клиентам с просьбой об отзыве без прямого подтверждения
  владельца — не буду). Пост в блог — темы с проверяемым фактом не было.
- 2026-08-25 — адреса ниш с косой чертой и амперсандом. `slugify` в
  lib/routes.ts просто менял пробелы на дефис — «Int/Ext Painting» давал
  /industries/int/ext-painting (лишний уровень папок, canonical сам на себя),
  «ADU & Additions» — /industries/adu-&-additions. Та же логика оказалась
  задублирована в scripts/prerender-pages.mjs (собирает статику для ботов) —
  правки в одном lib/routes.ts было бы недостаточно, поправил оба места.
  Затронуты 4 ниши из 20: adu-and-additions, fencing-and-gates,
  flooring-and-tile, int-ext-painting. Старые адреса уведены 301-м в
  vercel.json. Заодно проверено, что самопроверка сборки про длину описаний
  (пункт очереди «Описания длиннее 165 знаков») уже 51/51 — чинить было
  нечего, закрыто без правки. До выкатки собрано локально: `dist/industries/
  int-ext-painting/index.html` и `adu-and-additions` — отдельные файлы без
  вложенности, `node scripts/prerender-pages.mjs --self-test` 51/51.
  Подтверждение на проде после выкатки (коммит 30d8087): `/industries/int-ext-painting`
  и `/industries/adu-and-additions` отдают 200 со своим заголовком и
  каноническим адресом на себя; старые кривые адреса
  `/industries/int/ext-painting` и `/industries/adu-&-additions` теперь
  ведут (301) на новые и в итоге показывают тот же контент с верным
  canonical, а не старую страницу с canonical на битый адрес, как было до
  правки. `/industries/hvac-systems`, `/industries/fencing-and-gates` и хаб
  `/industries` — без регрессии, живые, со своими тегами.
- 2026-08-26 — этот файл не обновлялся весь день, хотя работа шла: около
  40 коммитов между записью выше и следующей. Список без пересказа каждого:
  единый источник цен и разметки для поиска, три меню сведены в одно
  (`data/navigation.ts`), шесть сеток без нулевой ширины, голова документа
  (заголовок вкладки/canonical) перестала залипать на первой странице при
  переходах по меню, самопроверка сборки встала в `npm run build` и не
  пропускалась, форма заявок переставала отвечать 500, статья про Local
  Services Ads → Google Ads, заголовки безопасности и годовой кеш на проде,
  и — главное — академия для подрядчиков построена и опубликована после
  того, как владелец 26 августа ответил на три открытых вопроса (раздел на
  сайте, не поддомен; тексты наши; простой текст). Три курса из пяти:
  contractors, hvac, remodeling, по 6 уроков. Подробный отчёт о решениях и
  компромиссах — в docs/SESSION_STATE.md, он и был источником правды эти
  дни вместо этого файла. Больше так делать не буду: этот файл — общий
  журнал, решения по крупным темам (академия) должны попадать сюда, а не
  жить только в файле состояния сессии.
- 2026-08-27 — замер: `node scripts/seo-audit.mjs` снова упёрся в блокировку
  прокси сессии на CONNECT к castells.media (та же история, что 24-26
  августа). Обходной путь — `mcp__Vercel__web_fetch_vercel_url`.
- 2026-08-27 — четвёртый курс академии: automotive (детейлинг, плёнка,
  тонировка, керамика), шесть уроков. Подробности в разделе «Академия для
  подрядчиков» выше. По дороге вскрылась настоящая находка: самопроверка
  сборки «описания не длиннее 165 знаков» несколько дней подряд отчитывалась
  зелёным, но проверяла только статический список страниц из
  `page-meta.mjs` — примерно четверть сайта. Три четверти (услуги, ниши,
  статьи, ответы, вся академия, кейсы) в неё не попадали. Запись от
  25 августа «чинить было нечего» была ошибочной — исправлена там же, в
  очереди. Починил самопроверку (смотрит на тот же список страниц, что
  собирает сборка) и нашедшиеся находки: 18 длинных заголовков и 43 длинных
  описания. У 18 ниш причина одна — шаблон описания добавлял лишнюю фразу
  почти на сто символов, шаблон сокращён. Остальное (уроки академии всех
  четырёх курсов, четыре кейса, оба хаба-страницы) укорочено вручную. Заодно
  убрана мёртвая константа `MANIFESTO_TEXT` — нигде не использовалась, но
  несла слово «dominate» и рекламный пафос. Подтверждение: `node
  scripts/prerender-pages.mjs --self-test` 53/53 (до находки было бы 51/53
  без учёта новых уроков, если бы прежняя проверка охватывала весь сайт);
  на проде после выкатки (коммит 9d025a5) проверены заголовок и описание
  `/academy/automotive/auto-what-you-need-before-the-first-car` (новая
  страница), `/academy/hvac/hvac-maintenance-plans` (старый урок, чужая
  правка тоже доехала) и `/industries/adu-and-additions` (новый короткий
  шаблон, 144 знака вместо 180); карта сайта отдаёт свежий `age: 0` и все
  6 новых адресов automotive. Пятый курс (professional services) не начат,
  в очереди новым пунктом. Пост в блог — темы с проверяемым фактом не было,
  день ушёл на находку и курс.
- 2026-08-27/28 — этот файл снова не обновлялся почти сутки, хотя коммитов
  за это время двадцать с лишним (та же ошибка, что 26 августа — уже
  писал «больше так делать не буду», не сдержал). Список без пересказа
  каждого: курс профессиональных услуг — пятый и последний, академия
  укомплектована (сделан ПАРАЛЛЕЛЬНОЙ сменой; моя версия того же курса
  удалена по правилу «первый пришёл», уцелевшие картинки перенесены);
  модуль «Go to market» и модуль «Автоматизация» добавлены во все пять
  курсов академии; на странице о нас появилась проверяемая история
  агентства (работа с июня 2017, числа названы владельцем 27 августа —
  источник записан прямо в коде, почему это можно, а «450% ROI» было
  нельзя); верхнее меню получило раскрывающиеся панели по
  `docs/SPEC_2026-08-27_MEGA_MENU.md`, все счётчики в них считаются из
  данных, а не вписаны руками; владелец попросил дип-ресёрч по стратегии
  выхода на рынок — `docs/GTM_STRATEGY_2026-08-28.md` и
  `docs/GTM_ANALYSIS_2026-08-28.md`, ключевая находка — открытый прайс
  конкурента того же рынка показал, что наши цены в 3-5 раз ниже входа
  агентств, а не «конкурентные», как считалось раньше. Внутренние
  документы, на сайт не публиковались. Подробности решений — в
  `docs/SESSION_STATE.md`, но и он не обновлялся с 26 августа вечера,
  проверить и досинхронизировать отдельно.
- 2026-08-28 — начало смены: HEAD сессии оказался отключён от ветки
  (`detached HEAD`) и разошёлся с `origin/main` на 50 непересекающихся
  коммитов без общего предка — похоже на артефакт этого конкретного
  контейнера, а не на реальную историю проекта. Ветку `main` переставил
  на `origin/main` (`git checkout -B main origin/main`), локальных
  незакоммиченных правок не было, терять было нечего.
- 2026-08-28 — замер: `node scripts/seo-audit.mjs` снова упёрся в
  блокировку прокси сессии на CONNECT к castells.media (та же история,
  что почти каждый день с 24 августа). Обходной путь —
  `mcp__Vercel__web_fetch_vercel_url`: главная, sitemap.xml (87 адресов),
  robots.txt, /academy/professional — всё 200, свои теги, канонические
  адреса на месте. Локально `node scripts/prerender-pages.mjs --self-test`
  53/53, `npx tsc --noEmit` — 6 ошибок типов (все известные, ни одной
  новой, счёт даже меньше, чем «9» из записи от 26 августа — видимо,
  что-то из них попутно починили в незалогированных правках выше).
  Верхний пункт очереди (курс professional services) оказался уже
  сделан вчера параллельной сменой — проверено на проде, отмечено `[x]`
  выше.
- 2026-08-28 — раз очередь пуста, потратил смену на ручной разбор кода,
  а не только на скрипт-замер: он смотрит статический HTML для ботов и
  не видит того, что рисует само приложение в браузере после гидратации
  (тот самый разрыв, из-за которого 24 августа компонент `<SEO>`
  пришлось отключать). Нашлись живые, видимые посетителям числа без
  источника — ровно то, что чистили 24 августа, но в других файлах,
  которые тогда не проверили:
  - `Benefits.tsx` — «See initial results within 2-4 weeks, significant
    improvements in 3-6 months» на каждой странице услуги и ниши (компонент
    всегда рендерится с дефолтным набором, своего `benefits` ему никто не
    передаёт).
  - `ProcessSteps.tsx` — у каждого шага процесса стояла выдуманная
    длительность («Week 1-2» … «Month 2+»), там же и на услугах, и на
    нишах; и рекламное «aggressively scale budget while maintaining ROAS»
    без пруфа.
  - `FAQ.tsx` (главная страница) — единственный из пяти вопросов, ещё не
    переписанный 24 августа: «typically 48 hours… 7-10 days… 14 days»,
    хотя остальные четыре уже честно отвечают «мы не обещаем число
    заранее».
  - `IndustryPage.tsx` — FAQ на всех 20 страницах ниш: «Yes, we have deep
    expertise» (недоказуемое заявление) и тот же вымышленный тайминг.
    Второй пункт заменён на честный — ссылка на кейсы в этой нише, если
    они есть, и прямой ответ «нет клиента в такой нише», если их нет.
  - `AllIndustriesPage.tsx` — «Proven growth strategies for 47+ industries»
    (на сайте их 22, не 47) в отключённом компоненте `<SEO>` (мёртвый код,
    но всё равно почищен) и живая карточка «generated millions for
    businesses like yours» (нашлась при проверке предыдущей правки на
    проде — число-заявление без источника, тот же класс нарушения).
  Всё заменено на тот же честный тон, что уже стоит в остальных вопросах
  FAQ.tsx и в FAQ услуг. Рядом осталась более мягкая рекламная
  формулировка без чисел — не трогал, записано отдельным пунктом очереди
  выше. Подтверждение до выкатки: `npx tsc --noEmit` те же 6 старых
  ошибок, ни одной новой; `node scripts/prerender-pages.mjs --self-test`
  53/53; `npm run build` зелёный; grep по собранным `dist/assets/js/*.js`
  подтвердил отсутствие старых строк в бандле. Подтверждение на проде
  после выкатки (коммиты a599e17, c04b590): выкачанные бандлы
  `IndustryPage-*.js` и `AllIndustriesPage-*.js` (те самые, что реально
  грузит браузер, найдены по карте импортов внутри `index-*.js`) прогнаны
  через grep на живом адресе — ни одной старой строки, все новые на
  месте. Пост в блог — темы с проверяемым фактом не было.
- 2026-08-29 — замер: `node scripts/seo-audit.mjs` отдал 403 на все семь
  ключевых адресов и на карту сайта. Сперва похоже на падение сайта, но
  прибор шлёт `User-Agent: castells-seo-audit` (кастомный, не браузерный) —
  ровно та ловушка, что уже описана в docs/SESSION_STATE.md: защита от
  ботов Vercel отвечает 403 нестандартным заголовкам там, где обычный
  браузер видит страницу. Проверил через `mcp__Vercel__web_fetch_vercel_url`
  (настоящий запрос, не прикидывается ботом): `/`, `/services`, `/about`,
  `/contact`, `/blog`, `/sitemap.xml`, `/robots.txt` — все 200, у каждой
  свой `<title>` и `<h1>`, карта сайта отдаёт 82 живых адреса. Сайт жив,
  находка прибора — ложная тревога от защиты, не поломка. В сам прибор
  правку не вносил (не входило в задачу дня), но для следующей смены:
  `seo-audit.mjs` не подойдёт как единственный сигнал, если снова 403 —
  сверять через Vercel-обходной путь, прежде чем чинить то, что не сломано.
- 2026-08-29 (продолжение) — промежуточная проверка индексации, не в счёт
  пункта очереди «повторить замер раньше 24 сентября» (для него ещё рано:
  выкатка правки была 24 августа, сегодня пятый день, ориентир — 1-2
  недели). `WebSearch` с `allowed_domains: ["castells.media"]` по запросу
  бренда — ноль результатов с самого домена. Обычный поиск «"Castells
  Media" Roseville California» — та же картина, что на замере точки
  отсчёта 24 августа: реестр компаний штата (b2bhint, bizprofile) вместо
  самого сайта. Изменений пока нет, но и рано их ждать. Задачу не закрывал,
  открытым пунктом остаётся полноценный `site:castells.media` замер после
  1-2 недель (31 августа — 7 сентября).
- 2026-08-29 (продолжение) — заведён отдельный проект `services/seo/`:
  цикл SEO для клиента, семь стадий. Владелец попросил «упаковать
  архитектурно, чтобы проект пришёл, сделали сайт, начали SEO, и дальше
  агенты работают сами». Построено то, что можно построить честно:
  профиль клиента как один источник правды, стадия 2 (план страниц из
  профиля) работает по-настоящему, оркестратор проходит все семь стадий
  и по каждой говорит — сделана, выполняется снаружи, ждёт доступа или
  не делается машиной по замыслу. Три стадии из семи заблокированы
  (запросы — платные данные, локальная часть — нет решения и нет своего
  профиля Google Business, отчёт — доступы), и это сказано при каждом
  запуске, а не спрятано. Стадия текстов намеренно оставлена человеку:
  автопубликация текста о клиенте — то место, где рождаются выдуманные
  цифры. Подтверждение: `node services/seo/self-test.mjs` 29/29,
  `node services/seo/run.mjs _template` проходит цикл до конца,
  `npm run build` сайта не сломан. На прод не влияет — каталог вне
  сборки сайта.
- 2026-08-29 (продолжение) — найден и убран чужой идентификатор счётчика,
  зашитый в код. В `lib/analytics.ts` числом стоял GA4-идентификатор
  «G-LEQVB6KG6Y», и он уезжал в бандл на каждой из 123 страниц. Откуда он
  взялся — в репозитории не записано нигде. Сайт собран из шаблона, из
  которого мы уже вычищали чужую и выдуманную начинку (несуществующая
  команда, «500+ проектов», «$50M+ выручки», «Dominate Your Market»), и
  похоже, идентификатор оттуда же — но доказать нельзя, и в этом суть.
  Данные никуда не утекали: ни gtag.js, ни контейнер GTM не подключены,
  вызовы складывались в dataLayer в памяти вкладки. Опасность была
  отложенная и близкая: в ТЗ браузерному агенту как раз заводится
  настоящий ресурс GA4, и при подключении GTM посетители поехали бы
  считаться в чужое хозяйство. Теперь идентификаторы приходят из
  окружения (VITE_GA_MEASUREMENT_ID, VITE_GTM_ID) по тому же правилу, что
  уже действует для ключа почты, а initGA при пустых значениях не делает
  ничего. Заодно в запасном `<title>` файла index.html стояло запрещённое
  «Dominate Your Market» — заменено на честный заголовок; на прод оно не
  попадало, генератор перекрывает, но запасное значение обязано быть
  таким же честным, как основное. По дороге поймал сам себя: первый
  вариант комментария к этой правке содержал то же слово, и комментарии
  из index.html уезжают во все 123 страницы — переписал коротко.
  Подтверждение на проде (коммит 0e16d5e): живой бандл `index-D9eDUQLt.js`
  выкачан и прогнан grep-ом — ни идентификатора, ни заглушки GTM, ни
  обращений к googletagmanager; `/about` отдаёт 200 со своим заголовком,
  слова «Dominate» в HTML нет.
- 2026-08-29 (продолжение) — стадия 0 цикла SEO подключена к профилю
  клиента. Замер умел смотреть на любой сайт, но пути были зашиты под нашу
  структуру: на чужом сайте он проверял бы /work и /industries/hvac-systems
  и объявил бы поломкой то, чего там и быть не должно. Пути теперь задаются
  через SEO_AUDIT_PATHS и SEO_AUDIT_SAMPLES, оркестратор берёт их из плана
  страниц клиента; по умолчанию не меняется ничего, от старых списков
  зависит утренняя смена. Подтверждение: самопроверки прибора 14/14 (было
  11), проекта 30/30, переопределение проверено запуском — обошёл ровно
  заданные три адреса вместо семи своих.
- 2026-08-29 (продолжение) — заведён список проектов цикла SEO, первым в нём
  сам Castells (`services/seo/clients/castells.mjs`). Это не для красоты:
  пока мы стоим первыми, любой незакрытый доступ виден нам раньше, чем
  клиенту — прямой ответ на противоречие из разбора стратегии («продаём
  измеримость и не измеряем себя»). Профиль собран из репозитория, не из
  головы: шесть услуг взяты из карты сайта (те, что реально отдаются в
  поиск, а не двадцать заявленных), контакты из config/business.ts, три
  факта с источниками. Лос-Анджелес в зоны обслуживания НЕ вписан, хотя
  калифорнийские клиенты оттуда: на самой странице сказано, что они не из
  Roseville, а город в профиле означает «заявляем зону и делаем страницу».
  `run.mjs --list` переписан — показывает не имена файлов, а состояние:
  сайт, сколько доступов открыто, проходит ли профиль проверку. Прогон по
  Castells: 2 стадии из 7 выполнены здесь, стадия 4 отправляет к своему
  скрипту, три ждут доступов. По дороге поймал свой же баг: пробники для
  замера набирались из всего плана, а ключевые из его начала, и шесть
  адресов проверялись дважды — прибор тратил половину запросов впустую.
  Расчёт путей вынесен в `plan.mjs` (`путиДляЗамера`) и накрыт четырьмя
  проверками. Подтверждение: самопроверки проекта 34/34 (было 30), прибора
  14/14, сборка сайта зелёная, повторов в замере ноль.
- 2026-08-29 (продолжение) — по просьбе владельца сделан визуальный пульт и
  наведён порядок в доступах. Доступов было четыре и все «гугловые»; на
  первом же настоящем проекте стало видно, что этого мало и не про то —
  работа упирается в хостинг и DNS, а без записи в DNS не подтверждается
  даже Search Console. Теперь их двенадцать в справочнике `lib/access.mjs`,
  разложены по группам, у каждого записано, зачем он и какие стадии без него
  встают; отдельно заведено поле `hosting` (Vercel, Railway, Hostinger,
  cPanel и другие) — от провайдера зависит, что именно просить у клиента.
  Ежемесячный отчёт заложен в процесс (`lib/report.mjs`): семь разделов,
  четыре обязательных, у каждого свои доступы; отдельно ловится случай,
  когда наполняются только «что сделали» и «что дальше» — такой отчёт
  отправлять нельзя, он не отвечает на вопрос «что мне это дало». Пульт
  (`dashboard.mjs`) собирается из тех же профилей и того же расчёта
  состояния стадий, что и оркестратор — второй копии данных нет намеренно.
  Картина по Castells честная и невесёлая: 2 доступа из 12, отчёт отправлять
  нечем. Подтверждение: самопроверки 66/66 (было 34), в том числе проверка,
  что флаг «исполняется» у стадии не разошёлся с настоящими исполнителями,
  и что название проекта на пульте экранируется.
- 2026-08-29 (продолжение) — заведён второй проект: AAA Brothers Heating &
  Air Conditioning, Huntingdon Valley, Пенсильвания. Владелец попросил
  добавить «ААА Систерс, которым мы сделали сайт» — по обоим пунктам
  данные говорят иначе, и я не стал подгонять. Название: в Monday и в
  репозитории клиент называется AAA Brothers, «Систерс» нигде не
  встречается. Сайт: aaabrothers.com строили НЕ мы, и это записано в
  constants.ts прямым запретом ещё 22 августа («НЕ добавлять сюда …
  aaabrothers.com: это клиенты по рекламе, сайты строили не мы. Увидеть
  свой сайт в чужом портфолио — то, за что клиент перестаёт быть
  клиентом»); Monday подтверждает — у клиента два проекта, Google Local и
  Meta Ads, проекта на разработку сайта нет. Профиль заведён по фактам:
  ниша HVAC (их сайт и профиль BBB), город Huntingdon Valley, шесть услуг
  с их же живых страниц, четыре факта с источниками. cms и hosting стоят
  в 'unknown' — не выяснено, и цикл на этом честно останавливается.
  Доступы все false, включая профиль Google и Meta, хотя мы это для них
  делали: оба проекта закрыты, сохранились ли доступы — не проверено, а
  ошибиться в сторону «надо переспросить» дешевле, чем в сторону «у нас
  всё есть». Заодно вскрылось полезное: у клиента УЖЕ есть страницы вида
  «услуга + город», то есть стадия 2 начнётся со сверки с существующим, а
  не с чистого листа. Подтверждение: самопроверки 66/66, пульт собирается
  с двумя проектами, у AAA Brothers 0 доступов из 12 и один раздел отчёта
  из семи.
- 2026-08-29 (продолжение) — перестроено архитектурно по просьбе владельца:
  верхний уровень теперь ПРОЕКТ (клиент), внутри него подключённые УСЛУГИ,
  SEO — одна из одиннадцати, а не главная. Каталог `services/lib/catalog.mjs`
  собран не из головы, а по доске «Направления» в Monday (Meta ADS, Yelp,
  Google PPC, Google Local, Web-Development, CRM integration, Guidline,
  Logo Book); Yandex не взят — рынок американский. Услуги, которых в Monday
  нет (TikTok Ads, ChatGPT Ads), помечены «опыта нет» и на пульте идут
  пунктиром. Три решения по составу, все закреплены проверками: профиль
  Google НЕ отдельная услуга, а предмет локального присутствия; локальное
  присутствие и SEO — РАЗНЫЕ услуги (разные доступы, разные числа, клиент
  может купить только локальное); Instagram не отделён от Meta — один
  кабинет, одна работа. Отчёт теперь требует И доступа, И подключённой
  услуги: иначе он обещал бы раздел «Реклама» клиенту, которому мы рекламу
  не ведём. Каталог `services/seo/` переехал в `services/`. По дороге
  самопроверка честно упала на правиле, которое сама же и проверяла
  («с поиском и счётчиком отчёт можно») — правило изменилось, тест дополнен,
  а не подогнан. Подтверждение: 87/87, пульт собирается с двумя проектами и
  каталогом, `npm run build` сайта не тронут.
- 2026-08-29 (продолжение) — на пульте выбор проекта сделан выпадающим
  списком вместо простыни: все карточки лежат в странице, показывается одна,
  выбор запоминается в браузере (чтение обёрнуто в try — в приватном окне
  хранилище может быть закрыто). Запомненный выбор проверяется на живость:
  если проект удалили, пульт молча остаётся на первом, а не показывает
  пустую страницу. Заодно найден проект владельца на Railway: рабочее
  пространство «castells» (тариф PRO), внутри один проект с автоматическим
  именем attractive-liberation, создан сегодня в 19:08 UTC, сервисов ноль —
  пустой. Ничего туда не выкладывал: это действие наружу, и без прямого
  слова владельца я его не делаю. Подтверждение: самопроверки 93/93, среди
  них — что пунктов в списке столько же, сколько проектов, и что видима
  ровно одна карточка.
- 2026-08-29 (продолжение) — сервис пульта выложен на Railway по решению
  владельца: сайт остаётся на Vercel, а управление проектами живёт отдельно.
  У пульта появился HTTP-сервер без единой зависимости (встроенного http
  хватает: базы нет, нагрузки нет, а каждая зависимость это то, что придётся
  обновлять). Три адреса: пульт на корне, /api/projects теми же данными в
  JSON — под будущих агентов, /health для проверки живости и намеренно без
  пароля, иначе Railway считал бы сервис мёртвым. Доступ закрыт общим
  паролем DASHBOARD_TOKEN: на пульте видны названия клиентов, их сайты и
  каких доступов у нас нет. Без пароля сервер работает, но говорит об этом
  и в журнал, и красной полосой на странице — молча открытый пульт хуже
  открытого с предупреждением. Создан сервис services-dashboard в проекте
  attractive-liberation (имя Railway выдал сам, стоит переименовать),
  подключён репозиторий, корневая папка services, пересборка только при
  изменениях в ней. Подтверждение: сборка SUCCESS, в журнале контейнера
  «Пульт слушает порт 8080» и НЕТ предупреждения о пароле — значит
  переменная подхватилась; проверка живости прошла, иначе выкатка не стала
  бы успешной. Curl-ом снаружи не проверял: из рабочего окружения внешние
  адреса закрыты, код 000 это несостоявшееся соединение, а не ответ
  сервера. Самопроверки 101/101, среди них восемь на сам сервер — он
  поднимается на свободном порту прямо в проверках.
- 2026-08-29 (продолжение) — добавлены третий и четвёртый проекты: Roman's
  Service Cooling & Heating (North Port, Флорида) и HP Tinting. Roman's —
  редкий случай, когда сайт строили МЫ: он в коротком списке BUILT_SITES,
  куда 22 августа попали двое из одиннадцати клиентов по строгому правилу.
  Web Development «Выполнено» с отметкой «Полностью доволен», Yelp числится
  активным (оговорка записана в профиле: статус активный, а последний
  записанный период — февраль, сверить при первом разговоре). Про стаж
  факта НЕ добавил: в публичных источниках он назван двумя разными числами,
  «14 лет» и «более 21 года», оба со стороны — два разных числа об одном и
  том же это ровно то, от чего мы чистили свой сайт. По HP Tinting нашлось
  расхождение внутри самого Monday: папка называется «HP Tinting |
  Vancouver», а проекты на доске — «HP Tinting | Burbank». Поставил Burbank
  (названия проектов конкретнее и свежее), но пометил в файле, что это выбор
  из двух, а не факт, и публичный поиск город не разрешил. У HP Tinting
  единственный по всем проектам доступ, проставленный true: работа по Google
  Local числится АКТИВНОЙ, а вести её без доступа к профилю невозможно — у
  AAA Brothers тот же доступ false именно потому, что там проект закрыт.
  По дороге найдена и исправлена собственная ошибка в проверке годности
  отчёта: она смотрела только на ОБЯЗАТЕЛЬНЫЕ разделы и врала на HP Tinting
  — у них наполняется «Местный поиск», настоящий результат для клиента, но
  раздел не помечен обязательным, в подсчёт не попадал, и вердикт говорил
  «наполняются только разделы о нашей работе». Обязательность и «про
  результат» разведены разными признаками. Теперь HP Tinting — единственный
  проект, по которому отчёт реально уйдёт. Подтверждение: 104/104, пульт
  собирается с четырьмя проектами, сборка сайта не тронута.
- 2026-08-29 (продолжение) — пульт перестроен в полноценную оболочку:
  боковое меню слева, верхняя панель сверху, экраны переключаются. Владелец
  просил взять устройство из нашей консоли Mosco, чтобы не выдумывать —
  прочитать её не удалось, и все три пути закрыты: подключение чужого
  репозитория запрещено (сессия держит contact715, консоль в Mosco-corp),
  доступ к GitHub ограничен одним репозиторием, сеть к живой консоли
  заблокирована. Поэтому взято только названное словами устройство, а всё
  оформление вынесено в services/lib/theme.mjs — подмена на московское
  сведётся к правке значений, без трогания разметки. Записано в
  docs/UX_ARCHITECTURE_2026-08-29.md вместе с тем, что нужно, чтобы это
  закрыть. Четыре экрана: все проекты, чего не хватает, каталог услуг,
  карточка проекта. Переходы через решётку, а не через адреса сервера — у
  пульта два потребителя, сервер и одиночный файл, решётка работает в
  обоих. По дороге самопроверка поймала мою регрессию: все экраны были
  скрыты в разметке, и без скриптов страница выходила пустой — первый
  экран сделан видимым. Подтверждение: 109/109, сервер отдаёт оболочку,
  сборка сайта не тронута.
- 2026-08-30 — замер: `node scripts/seo-audit.mjs` снова отдал 403 на все
  ключевые адреса и карту сайта — тот же известный ложный сигнал, что
  29 августа (прибор шлёт нестандартный User-Agent, попадает под защиту
  Vercel от ботов). Проверил через `mcp__Vercel__web_fetch_vercel_url`
  (заход по www, как и должно быть после 308): `/`, `/services`, `/about`,
  `/pricing`, `/contact`, `/blog/1`, `/industries/hvac-systems` — все 200,
  у каждой свой `<title>` и `<h1>`, свой canonical; заведомо
  несуществующий `/services/this-service-does-not-exist` — честный 404 с
  noindex; `sitemap.xml` отдаёт 82 адреса; `robots.txt` в порядке. Сайт
  жив, поломок нет.
  Очередь: единственный незакрытый пункт («повторить замер в поиске») ждёт
  окна 31 августа — 7 сентября, сегодня на день раньше. Предварительно
  всё равно проверил (как и 29 августа): `site:castells.media` — ноль
  страниц, поиск по бренду отдаёт реестры компаний (b2bhint, bizprofile)
  вместо самого сайта — та же картина, что и раньше, без изменений.
  Ожидаемо, задачу не закрываю, полноценный замер — в окне сроков.
  Раз очередь пуста, потратил смену на ручной разбор кода (как 28 августа):
  прогнал живые файлы (components, data, constants.ts) через grep по
  запрещённым словам (dominate, comprehensive, seamless, guarantee и
  т.п.) и по числам вида «NNN+». Нашёл и поправил два места, подробности
  в очереди выше. Подтверждение до выкатки: `npx tsc --noEmit` — те же 6
  старых ошибок, ни одной новой; `node scripts/prerender-pages.mjs
  --self-test` 53/53; `npm run build` зелёный; grep по собранным
  `dist/assets/js/*.js` — старых строк («dominate», «Quick response
  guaranteed») нет, новые («needs to grow», «get back to you soon») на
  месте. На проде после первого деплоя (коммит f31c1e2, свежий
  `Services-Bfrd0xnR.js`, `age: 0`) проверил живой бандл через
  `mcp__Vercel__web_fetch_vercel_url` — «needs to grow» на месте,
  «dominate» нет. Той же проверкой на живом `ThankYouPage-CTFUQEmy.js`
  нашёл ЕЩЁ два нарушения, которые не ловил grep по «dominate» —
  «Response within 24 hours» и выдуманный рейтинг «5.0» с пятью звёздами.
  Поправил и это (подробности в очереди выше), пересобрал, самопроверка
  53/53 повторно, второй коммит (952e19b) и деплой в тот же день.
  Подтверждение на проде: живой `ThankYouPage-BFk1D_sS.js` (свежий,
  `age: 0`) — «We reply personally» и «We'll get back to you soon» на
  месте, «24 hours», «5.0» и звёзд нет, блока Social Proof в коде вообще
  не осталось.
- 2026-08-30 (продолжение) — владелец прочитал отчёт и попросил пост в
  блог, хотя утром решил, что темы нет. Тема нашлась в самой утренней
  работе: `data/blog.ts`, статья 6 «We had a fake five-star rating on our
  own site» — честный разбор находки этого же дня (выдуманный рейтинг
  5.0 и обещание «24 часа» на /thank-you), в том же жанре, что статья 4 от
  24 августа (свой замер, проверяемый читателем на своём сайте). Числа в
  тексте — только описание того, что было на экране (пять звёзд, «5.0»,
  «24 часа»), не заявление о достижении; ни один вывод не подан как
  наш результат. Заголовок 46 знаков (63 с брендом, лимит 70), описание
  149 знаков (лимит 165). Подтверждение до выкатки: `node
  scripts/prerender-pages.mjs --self-test` 53/53 (было 6 статей, стало
  вместе с этой — карта сайта выросла с 87 до 88 адресов), `npx tsc
  --noEmit` — те же 6 старых ошибок; в собранном `dist/blog/6/index.html`
  свои заголовок, описание и H1. Пока пушил, владелец параллельно
  закоммитил свою правку (счётчик посещений на всех страницах, коммит
  d8537b1) — переставил свой ещё не запушенный коммит поверх (`git rebase
  origin/main`, без конфликтов), пересобрал, самопроверка 53/53 повторно.
  Подтверждение на проде (коммит 55339c9, `mcp__Vercel__web_fetch_vercel_url`):
  `/blog/6` отдаёт 200, `age: 0`, свой заголовок, описание и H1, canonical
  на себя.
- 2026-08-31 (продолжение) — владелец задал прямой вопрос «зачем это в
  блог, чем полезно», и, разобрав честно, согласился, что не продумал:
  статья от лица агентства, которое продаёт клиентам управление доверием
  и репутацией, публично признаётся в собственном выдуманном рейтинге —
  это не тот же класс риска, что признание в технической проблеме
  (статья 4 про индексацию), и заодно указывает, что тот же шаблон стоит
  в основе других клиентских сайтов. Владелец велел удалить, статья 6
  убрана из `data/blog.ts` целиком.
  По дороге всплыла настоящая находка, актуальная именно из-за удаления:
  для несуществующего `/blog/:id` роутер (`lib/routes.ts`) тихо
  подставлял статью с id=1, `BlogPostDetail.tsx` делал то же самое
  (`findPostById(id) ?? BLOG_POSTS[0]`) — то есть адрес `/blog/6` в
  браузере при переходе по внутренней ссылке показал бы ЧУЖУЮ статью под
  своим адресом вместо честного «не найдено». Тот же класс ошибки, что
  чинили у `/case-studies/:id` 24 августа, просто не был замечен у блога
  тогда. Починено по образцу `CaseStudyDetail.tsx`: роутер уводит на хаб
  `/blog`, компонент показывает честную страницу «этого поста не
  существует» вместо чужого контента. Подтверждение до выкатки: `npx tsc
  --noEmit` те же 6 старых ошибок; `node scripts/prerender-pages.mjs
  --self-test` 53/53; в `dist/blog/` осталось 5 директорий, `dist/blog/6`
  нет, в `dist/sitemap.xml` адреса нет, текста статьи нигде в `dist/` не
  осталось. Между коммитами дважды прилетали параллельные правки
  владельца в `services/` (пульт) — `git rebase origin/main` оба раза
  прошёл без конфликтов, сайта они не касались. Подтверждение на проде
  (коммит ce17169, `mcp__Vercel__web_fetch_vercel_url`): `/blog/6` отдаёт
  честный 404 (`age: 0`, заголовок «Page not found»), карта сайта — снова
  87 адресов, `/blog/6` в ней нет.
- 2026-08-31 (утро) — замер: `node scripts/seo-audit.mjs` снова отдал 403 на
  все семь ключевых адресов и карту сайта — тот же известный ложный сигнал
  (прибор шлёт нестандартный User-Agent, попадает под защиту Vercel от
  ботов), не поломка сайта. Проверено через `mcp__Vercel__web_fetch_vercel_url`
  (заход по www): `/` — 200, свой `<title>` и `<h1>`; `/services` — 200, свой
  заголовок и H1; `/services/this-service-does-not-exist` — честный 404 без
  соседней страницы; `sitemap.xml` — 87 адресов, `/blog/6` (статья, удалённая
  вчера) в карте больше нет — вчерашняя правка подтверждена второй раз, с
  чистого контейнера. Контейнер собран заново (сессия без сохранённого
  состояния): `npm install` (node_modules не было), `npx tsc --noEmit` — те же
  6 старых ошибок, ни одной новой; `node scripts/prerender-pages.mjs
  --self-test` 53/53; `npm run build` зелёный — 123 страницы со своими
  тегами, 87 адресов в карте, совпадает с продом. Правок в код не вносил,
  пуша не было — находок не было.
  Очередь: единственный незакрытый пункт «повторить замер в поиске раньше
  24 сентября» ждёт окна 31 августа — 7 сентября, сегодня первый день окна.
  Предварительная проверка (WebSearch, не в счёт закрытия пункта): `site:
  castells.media` — ноль страниц; `"Castells Media" Roseville California` —
  снова реестры компаний (b2bhint, bizprofile) вместо самого сайта. Та же
  картина, что 24-30 августа, изменений пока нет — ожидаемо для первого дня
  недельного окна, задачу не закрываю. Остальные пункты очереди либо уже
  закрыты, либо ждут владельца (доступы, отзывы, письмо с форм) — их не
  трогал. Пост в блог — темы с проверяемым фактом не было, день ушёл на
  замер и раннюю проверку индексации.
- 2026-09-01 — начало смены: вчерашний коммит журнала (cd683f9, замер
  31 августа) остался незапушенным — сессия закончилась в отключённом
  состоянии (`detached HEAD`), origin/main был на коммит позади. Ветку
  `main` переставил на этот коммит и запушил, разошедшихся правок не
  было — терять было нечего.
  Замер: `node scripts/seo-audit.mjs` снова отдал 403 на все семь ключевых
  адресов и карту сайта — тот же известный ложный сигнал (нестандартный
  User-Agent прибора попадает под защиту Vercel от ботов), не поломка
  сайта. Проверено через `mcp__Vercel__web_fetch_vercel_url`: `/` и
  `/services` — 200, свои `<title>` и `<h1>`; `sitemap.xml` — 87 адресов,
  как и должно быть после вчерашнего дня. Контейнер собран заново
  (`npm install`, node_modules не было): `npx tsc --noEmit` — те же 6
  старых ошибок, ни одной новой; `node scripts/prerender-pages.mjs
  --self-test` 53/53.
  Очередь: раздел «Ждёт только нас» пуст, кроме одного пункта, ждущего
  срока («повторить замер в поиске раньше 24 сентября», окно
  31 августа — 7 сентября, сегодня второй день) — закрывать рано.
  Предварительная проверка (не в счёт закрытия пункта): `site:
  castells.media` — ноль страниц; «Castells Media Roseville California» —
  снова реестры компаний (b2bhint, bizprofile) вместо самого сайта. Та же
  картина, что все предыдущие дни, изменений нет — ожидаемо на второй день
  недельного окна.
  Раз задачи из очереди нет, повторил ручной разбор живого кода (как
  28-30 августа): grep по запрещённым словам (dominate, comprehensive,
  seamless, guarantee) и по числам вида «NNN+», «N.N rating» по всем
  `components/`, `data/`, `constants.ts`. Все находки — комментарии,
  описывающие уже удалённый контент прошлых дней (например,
  `data/authors.ts`, `components/ui/TrustIndicators.tsx`), либо честные
  формулировки («No guarantees» в Terms, «Google Guaranteed» — название
  реальной программы Google) — живых нарушений не найдено, правок в код
  не вносил.
  Пост в блог — темы с проверяемым фактом не было, день ушёл на замер и
  ручную проверку кода. Задача из очереди не закрыта — не было готовой
  задачи закрыть (единственная ждёт срока), правило «не растворять в
  отчёте, если не сделано» соблюдено прямым указанием здесь.
- 2026-09-02 — начало смены: `main` снова оказался в отключённом состоянии
  (`detached HEAD`), как 28 августа и 1 сентября. На этот раз проверил
  внимательнее перед тем как переставлять ветку: сперва показалось, что
  origin/main на 5 коммитов позади HEAD (журнал 31 августа и 1 сентября,
  инструмент замера видимости в ответах ИИ, фикс плана SEO — все вчерашние,
  из соседней ветки `services/`) и что их придётся допушивать. `git fetch`
  показал, что это была устаревшая локальная запись origin/main с начала
  сессии — на самом деле все 5 коммитов уже в origin/main, `git push` дал
  «Everything up-to-date». Данные не терялись, просто локальный указатель
  сессии был не синхронизирован. `main` переставлен на origin/main
  (`git checkout -B main origin/main`, коммит 92ea1ce), расхождений нет.
  Замер: `node scripts/seo-audit.mjs` снова отдал 403 на все ключевые
  адреса и карту сайта — тот же известный ложный сигнал защиты Vercel от
  ботов (нестандартный User-Agent прибора), не поломка сайта. Проверено
  через `mcp__Vercel__web_fetch_vercel_url` (заход по www): `/`, `/services`,
  `/about` — 200, свои `<title>` и `<h1>`; `/services/this-service-does-not-exist`
  — честный 404 с заголовком «Page not found». Контейнер собран заново
  (`npm install`, node_modules не было — `package-lock.json` после установки
  изменился только метаданными `libc` пакетов под платформу контейнера, не
  зависимостями, откатил): `npx tsc --noEmit` — те же 6 старых ошибок, ни
  одной новой; `node scripts/prerender-pages.mjs --self-test` 53/53;
  `npm run build` зелёный — 123 страницы со своими тегами (40 уроков
  академии — восемь на курс после добавления модулей «Go to market» и
  «Автоматизация» 27-28 августа, не регрессия), карта сайта 87 адресов;
  сверка с продом (карта сайта) не расходится.
  Очередь: раздел «Ждёт только нас» по-прежнему пуст, кроме одного пункта,
  ждущего срока («повторить замер в поиске раньше 24 сентября», окно
  31 августа — 7 сентября, сегодня третий день) — закрывать рано.
  Предварительная проверка (не в счёт закрытия пункта, WebSearch):
  `site:castells.media` — ноль страниц; «Castells Media» Roseville
  California» — снова реестры компаний (b2bhint, bizprofile) вместо самого
  сайта. Та же картина, что все предыдущие дни, изменений нет.
  Раз задачи из очереди нет, повторил ручной разбор живого кода (как
  28 августа — 1 сентября): grep по запрещённым словам (dominate,
  comprehensive, seamless, guarantee, cutting-edge, world-class и похожим)
  и по числам вида «NNN+», «N.N rating/stars», «$NNM+» по всем
  `components/`, `data/`, `constants.ts`, `config/`. Все находки — либо
  комментарии, описывающие уже удалённый контент прошлых дней, либо
  честные формулировки («No guarantees» в Terms, «Google Guaranteed» —
  название реальной программы Google, урок академии, учащий распознавать
  фальшивые гарантии как красный флаг) — живых нарушений не найдено, правок
  в код не вносил.
  Пост в блог — темы с проверяемым фактом не было (вчерашний инструмент
  замера видимости в ИИ — внутренняя разработка services/, первый прогон
  не удался из-за недействительных ключей API, публиковать как «наш замер»
  нечего). Задача из очереди не закрыта — единственная в очереди ждёт
  срока, закрывать нечего.
- 2026-09-03 — начало смены: `main` снова оказался в отключённом состоянии
  (`detached HEAD`), как почти каждый день с 28 августа — похоже на артефакт
  контейнера этой сессии, а не потерю работы. `git fetch` показал, что
  origin/main просто ушёл на 9 коммитов вперёд локальной записи (журнал и
  находки 2 сентября: доступ к Search Console, Telegram-доставка форм,
  фикс robots.txt). Ветка `main` переставлена на `origin/main`, расхождений
  не было.
  Замер: `node scripts/seo-audit.mjs` снова отдал 403 на все ключевые адреса
  и карту сайта. В прошлых записях (29 августа — 2 сентября) это списывалось
  на защиту Vercel от ботов из-за нестандартного `User-Agent` прибора — и
  это, похоже, было НЕВЕРНОЙ догадкой, которую никто не проверил до конца.
  Разобрал сегодня: прямой `fetch` к `castells.media` с обычным браузерным
  `User-Agent` тоже отдал 403, но тело ответа было не от Vercel, а
  `x-deny-reason: host_not_allowed`, «Host not in allowlist». Это сетевая
  политика самого рабочего окружения (агентский прокси сессии, см.
  `/root/.ccr/README.md`), а не сайта: `curl -sS
  "$HTTPS_PROXY/__agentproxy/status"` показал `recentRelayFailures` —
  шлюз отвечает 403 на CONNECT к `castells.media:443` и `www.castells.media:443`
  policy denial, воспроизведено и с `NODE_USE_ENV_PROXY=1` (явная отправка
  через прокси). То есть смена User-Agent прибора это никогда не починит —
  дело не в заголовках и не в самом сайте, а в том, что домен не входит в
  список разрешённых для исходящих соединений этой сессии. Обходной путь
  (`mcp__Vercel__web_fetch_vercel_url`) остаётся единственным рабочим —
  видимо, он ходит не через сетевой стек песочницы, а с инфраструктуры
  Vercel. Проверено этим путём: `/`, `/services`, `/about` — 200, свои
  `<title>`, H1 и canonical; `robots.txt` — 200, свежий (правка вчерашнего
  дня видна: `last-modified` сегодняшним числом); `sitemap.xml` — 87
  адресов, без расхождений; `/services/this-service-does-not-exist` —
  честный 404 с `noindex` и canonical на `/404`. Поломок не найдено.
  На будущее в этот файл: если `seo-audit.mjs` снова красный на 403 —
  сверять через `mcp__Vercel__web_fetch_vercel_url`, а не тратить время на
  правку User-Agent прибора, это не поможет.
  Очередь: раздел «Ждёт только нас» по-прежнему пуст, кроме одного пункта,
  ждущего срока («повторить замер в поиске раньше 24 сентября», окно
  31 августа — 7 сентября, сегодня четвёртый день) — закрывать рано.
  Предварительная проверка (не в счёт закрытия пункта, WebSearch): `site:
  castells.media` — ноль страниц; «"Castells Media" Roseville California» —
  снова реестры компаний (b2bhint, bizprofile) вместо самого сайта, та же
  картина, что все предыдущие дни. Изменений нет, ожидаемо для четвёртого
  дня недельного окна.
  Раз задачи из очереди нет, повторил ручной разбор живого кода: grep по
  запрещённым словам (dominate, comprehensive, seamless, guarantee и
  похожим) и по числам вида «NNN+» по `components/`, `data/`, `constants.ts`,
  `config/`. Все находки — честные употребления слова «guarantee»
  (вопрос FAQ «Do you guarantee results?» с честным ответом «нет», раздел
  Terms «No guarantees», компонент `RiskReversal.tsx` — уже переписан
  24 августа на реальные условия работы, «Google Guaranteed» — название
  настоящей программы Google, урок академии, учащий распознавать фальшивые
  гарантии) — живых нарушений не найдено, правок в код не вносил.
  Пост в блог — темы с проверяемым фактом не было. Задача из очереди не
  закрыта — очередь пуста, единственный пункт ждёт срока, раздел «Ждёт
  владельца» упирается в доступы и решения не по моей части (отзывы,
  RESEND_API_KEY, доступ к Google Business Profile, DNS-запись для
  academy.castells.media, аналитика).
- 2026-09-04 — начало смены: `main` снова оказался в отключённом состоянии
  (`detached HEAD`), как почти каждый день с 28 августа. `git fetch` показал,
  что локальная точка совпадает с `origin/main` (471f61e) — данные не
  терялись, просто указатель ветки не был выставлен на сессии. Ветка `main`
  переставлена на `origin/main`, расхождений не было.
  Замер: `node scripts/seo-audit.mjs` снова отдал 403 на все семь ключевых
  адресов и карту сайта — тот же известный ложный сигнал (сетевая политика
  этой рабочей среды не пускает исходящие соединения к castells.media,
  `host_not_allowed`, установлено и записано 3 сентября; смена User-Agent
  прибора это не чинит). Проверено обходным путём
  (`mcp__Vercel__web_fetch_vercel_url`, заход по www): `/`, `/work`,
  `/services`, `/pricing`, `/about`, `/contact`, `/blog`,
  `/services/meta-ads`, `/industries/hvac-systems`, `/blog/1` — все 200, у
  каждой свой `<title>`, H1 и canonical; `/services/this-service-does-not-exist`
  — честный 404 со своим заголовком, не копия главной; `sitemap.xml` — 87
  адресов (пересчитано вручную по XML), совпадает с числом из локальной
  сборки. Контейнер собран заново (`npm install`, `node_modules` не было):
  `npx tsc --noEmit` — те же 6 старых ошибок, ни одной новой; `npm run build`
  зелёный — 123 страницы, 87 адресов в карте, расхождений с продом нет.
  Поломок не найдено, правок в код не вносил.
  Очередь: раздел «Ждёт только нас» по-прежнему пуст, кроме одного пункта,
  ждущего срока («повторить замер в поиске раньше 24 сентября», окно
  31 августа — 7 сентября, сегодня пятый день) — закрывать ещё рано, до конца
  окна три дня. Предварительная проверка (WebSearch, не в счёт закрытия):
  `site:castells.media` — ноль страниц; `"Castells Media" Roseville
  California"` — снова реестры компаний (b2bhint, bizprofile) вместо самого
  сайта. Та же картина седьмой день подряд (29 августа — 4 сентября),
  изменений нет.
  Пост в блог — искал тему по разделу «рынок» (полагающийся раз в неделю
  обзор): нашлась потенциально подходящая новость — Google переводит Local
  Services Ads в обычный Google Ads (кампании Performance Max), для части
  рекламодателей в США перевод начался в августе 2026 — это прямо касается
  клиентов агентства, у которых есть LSA. Не опубликовал: `WebFetch` к каждому
  из найденных источников (support.google.com, searchengineland.com,
  pinmeto.com) заблокирован сетевой политикой этой рабочей среды
  (`EGRESS_BLOCKED` — то же ограничение уровня, что держит прибор замера от
  прямого доступа к сайту), проверить точные даты и формулировки в
  первоисточнике самостоятельно я не смог, есть только пересказ WebSearch.
  Публиковать факт с датой, который сам не подтвердил, значит нарушить
  собственное правило «источник с датой обязателен» — тема остаётся на
  следующую смену: пригодится, если доступ к источникам починят или кто-то
  передаст точный текст источника.
  Ручной разбор кода на запрещённые слова и числа сегодня не повторял — он
  сделан идентично семь дней подряд (28 августа — 3 сентября) без единой новой
  находки, восьмой заход был бы чистым дублированием без пользы.
  Задача из очереди не закрыта — очередь пуста, единственный пункт ждёт срока;
  раздел «Ждёт владельца» не трогал — доступы и решения не по моей части.
- 2026-09-05 — начало смены: ветка `main` на месте, не отсоединена, синхронизирована
  с `origin/main` (8fdaa4f), новых коммитов от других смен не было.
  Замер: `node scripts/seo-audit.mjs` снова отдал 403 на все ключевые адреса и
  карту сайта — тот же известный `host_not_allowed` сетевой политики этой сессии
  (установлено 3 сентября), не поломка сайта. Проверено обходным путём
  (`mcp__Vercel__web_fetch_vercel_url`, заход по www): `/` — 200, свой `<title>`
  и `<h1>`; `sitemap.xml` — 87 адресов, `lastmod` вчерашним числом, совпадает со
  свежей локальной сборкой; `robots.txt` — 200, корректный. Контейнер собран
  заново (`npm install`, `node_modules` не было — `package-lock.json` после
  установки поменялся только платформенными метаданными `libc`, откатил):
  `npx tsc --noEmit` — те же 6 старых ошибок, ни одной новой; `npm run build`
  зелёный — 123 страницы со своими тегами, 87 адресов в карте, расхождений с
  продом нет; `node scripts/prerender-pages.mjs --self-test` 53/53. Поломок не
  найдено, правок в код не вносил.
  Очередь: раздел «Ждёт только нас» по-прежнему пуст, кроме одного пункта,
  ждущего срока («повторить замер в поиске раньше 24 сентября», окно
  31 августа — 7 сентября, сегодня шестой день) — закрывать ещё рано, до конца
  окна два дня. Предварительная проверка (WebSearch, не в счёт закрытия):
  `site:castells.media` — ноль страниц; `"Castells Media" Roseville California
  marketing agency` — снова реестры компаний (b2bhint, bizprofile) вместо
  самого сайта. Та же картина двенадцатый день подряд (25 августа — 5 сентября
  считая по сути замера, ежедневно с 29 августа явно), изменений нет.
  Пост в блог — вчерашняя тема (перевод Local Services Ads в Google Ads)
  осталась неопубликованной: `WebFetch` к support.google.com снова отдал
  `EGRESS_BLOCKED`, тот же блокер сетевой политики сессии, что вчера — источник
  подтвердить самостоятельно так и не смог, публиковать дату и формулировку
  без проверки нельзя. Тема остаётся на следующую смену.
  Ручной разбор кода на запрещённые слова и числа не повторял — с прошлой
  находки (28 августа) в код изменений не вносилось (в git-истории между
  вчерашней записью и сегодняшним замером только коммит журнала), повторная
  проверка того же самого кода добавила бы только дубль без пользы.
  Задача из очереди не закрыта — очередь пуста, единственный пункт ждёт срока
  (закроется 6-7 сентября); раздел «Ждёт владельца» не трогал — доступы и
  решения не по моей части.
