# 📝 Настройка форм

## Варианты настройки

### Вариант 1: Resend API (Рекомендуется)

1. Зарегистрируйтесь на [Resend.com](https://resend.com)
2. Создайте API ключ
3. Создайте файл `.env` в корне проекта:
   ```
   VITE_RESEND_API_KEY=re_xxxxxxxxxxxxx
   ```
4. Перезапустите dev server

**Преимущества:**
- Простая настройка
- Надежная доставка
- Бесплатный план: 3000 emails/месяц

---

### Вариант 2: Собственный API endpoint

1. Создайте backend endpoint (например, `/api/contact`)
2. Добавьте в `.env`:
   ```
   VITE_FORM_API_ENDPOINT=https://your-api.com/api/contact
   ```
3. Ваш API должен принимать POST запрос с JSON:
   ```json
   {
     "name": "John Doe",
     "email": "john@example.com",
     "phone": "+1234567890",
     "topic": "web-design",
     "message": "Hello!"
   }
   ```
4. Возвращать JSON:
   ```json
   {
     "success": true,
     "message": "Form submitted successfully"
   }
   ```

---

### Вариант 3: Development режим

Если не настроен ни Resend, ни API endpoint, формы будут работать в development режиме:
- Данные выводятся в консоль
- Форма показывает success сообщение
- Для production нужно настроить один из вариантов выше

---

## Настройка email получателя

В файле `lib/api/forms.ts` измените email получателя:

```typescript
to: ['hello@castells.agency'], // Замените на ваш email
```

---

## Защита от спама

### Honeypot поле (рекомендуется)

Добавьте скрытое поле в формы:

```tsx
<input
  type="text"
  name="website_url"
  style={{ display: 'none' }}
  tabIndex={-1}
  autoComplete="off"
/>
```

Если это поле заполнено - это бот, игнорируйте отправку.

### Rate limiting

На backend добавьте ограничение:
- Максимум 3 отправки с одного IP в час
- Используйте Redis или аналоги для хранения счетчиков

### reCAPTCHA v3 (опционально)

Можно добавить Google reCAPTCHA v3 для дополнительной защиты.

---

## Тестирование

1. Заполните форму на сайте
2. Проверьте консоль браузера (в dev режиме)
3. Проверьте email (если настроен Resend)
4. Проверьте Google Analytics - должно быть событие `submit` (Form)

---

**Готово!** Формы настроены и готовы к использованию. 🎉

